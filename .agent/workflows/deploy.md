---
description: 프로젝트 필몽 배포 가이드 (Deployment Guide)
---

# 🚀 프로젝트 필몽 배포 워크플로우

개발 모드(`npm run dev`)가 아닌 실전 운영을 위해 최적화된 상태로 시스템을 가동하는 방법입니다.

## 1. 전제 조건
- Node.js 및 npm 설치 완료
- 데이터베이스 설계 반영 완료 (`npx prisma db push`)

## 2. 배포 단계

### Step 1: 의존성 설치
새로운 환경이라면 필요한 패키지들을 먼저 설치합니다.
```bash
npm install
```

### Step 2: 데이터베이스 준비
운영 환경에 맞는 데이터베이스 클라이언트를 생성합니다.
```bash
npx prisma generate
```

### Step 3: 프로덕션 빌드
Next.js 코드를 실행 가능한 최적화된 상태로 컴파일합니다.
```bash
npm run build
```

### Step 4: 서버 가동
빌드된 결과물을 사용하여 실제 서버를 실행합니다. (기본 3000 포트)
```bash
npm run start
```

## 3. 추천 운영 방식 (PM2 사용 시)
서버가 꺼졌을 때 자동으로 재시작되게 하려면 `pm2`를 추천합니다.

```bash
# pm2 설치
npm install -g pm2

# 서비스 시작
pm2 start npm --name "phil-mong" -- start
```

## 4. 체크리스트
- [ ] `.env` 파일에 필요한 환경 변수가 설정되어 있는가?
- [ ] `prisma/dev.db` 파일에 접근 권한이 있는가?
- [ ] 3000번 포트가 개방되어 있는가?

---
> [!TIP]
> **Vercel 배포 시 주의사항**: 현재 SQLite를 사용 중이므로, Vercel 같은 서버리스 환경보다는 일반 VPS(카페24, AWS EC2 등) 가비아 등의 환경에서 상시 구동하는 것이 데이터 유지에 유리합니다.
